# Copy this file to `.env.local` (in the same `frontend/` folder).
# These are non-secret URLs — safe to share, but the file should still
# be local-only so each developer can override.

NEXT_PUBLIC_API_URL=http://localhost:8000/api
BACKEND_URL=http://localhost:8000
